server:
  port: 9192
spring:
  h2:
    console:
      enabled: true

  application:
    name: ECOMMERCE   

      
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8084/eureka/

  instance:
    hostname: localhost
